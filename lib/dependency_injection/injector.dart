import 'package:get_it/get_it.dart';

final injector = GetIt.instance;

Future<void> initDependencies() async {
  // Register dependencies here
  // Example:
  // injector.registerLazySingleton<ExampleRepository>(() => ExampleRepositoryImpl());
}
